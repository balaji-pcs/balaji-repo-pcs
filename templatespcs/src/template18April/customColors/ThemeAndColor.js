import { createTheme } from "@mui/material";
import { red, teal, amber, green } from "@mui/material/colors";

export const redColor = red[700];
export const tealColor = teal[300];
export const amberColor = amber[500];
export const greenColor = green[500];

// export const theme = createTheme();

// theme.typography.caption = {
//   fontSize: "0.7rem",
// };

// theme.typography.body2 = {
//   fontSize: "0.8rem",
//   fontWeight: "bold",
// };

// theme.typography.overline = {
//   fontSize: "0.8rem",
//   fontWeight: "bold",
// };
